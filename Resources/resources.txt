User (
	id INTEGER,
	registered_no String,
	name String,
	mobile String,
	email String,
	role_id int,
	room_no String,
	is_deleted int
)

Login(
	id INTEGER,
	password String
)

Role (
	id INTEGER,
	role_name String
)

Category (
	id INTEGER,
	type String,
	is_deleted int
)

Product (
	id INTEGER,
	name String,
	category_id int
	is_hide int,
	is_deleted int
)

Inventory (
	id INTEGER,
	product_id int,
	quantity int,
	unit_price float,
	inserted_at LocalDateTime,
	expiry_at LocalDateTime,
	is_deleted int
)

Orders (
	id INTEGER,
	item_id int,
	quantity int,
	unit_price float,
	ordered_at LocalDateTime,
	user_id int,
	is_canceled int
)

Orders_log (
	id INTEGER,
	item_id int,
	user_id int,
	quantity int,
	total_price float,
	ordered_at LocalDateTime,
	delivered_at LocalDateTime
)

Transactions (
	transaction_date LocalDateTime,
	total_amount float
)